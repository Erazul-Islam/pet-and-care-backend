export type TComment = {
    // email:string,
    userId : string,
    userName : string
    userProfilePhoto : string,
    text : string,
    createdAt : Date,
    updatedAt : Date
}

export type TInfo = {
    userId: string,
    userName: string,
    userProfilePhoto: string
}